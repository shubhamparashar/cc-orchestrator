# CLAUDE.md

Guidance for Claude Code working in this repo (cc-orchestrator).

cc-orchestrator is a zero-dependency local control plane over Claude Code sessions
(dashboard, remote control, rolling per-session `context.md`, BM25 session index,
launcher, cost calc, always-on LaunchAgent, Tailscale phone access). Full design and
audit live in `REPORT.md`; usage in `README.md`. Read those before re-deriving anything.

## Handoff Log

Every time you finish a working session on cc-orchestrator and hand off to the next
agent, **append one row** to the table below and title the handoff doc with the
**Name** from that row.

- **Numbering — monotonic.** Take the highest session number in the table and add 1.
  Handing off from session 3 → the next is session 4. Never reuse a number.
- **Name format —** `cc-orchestrator — <YYYY-MM-DD> — session <N>`
  (e.g. `cc-orchestrator — 2026-06-17 — session 4`). Use this as the handoff doc's H1
  title, and save the file as `handoffs/session-<N>-<YYYY-MM-DD>.md` (link it in the
  **Doc** column below so the log stays a navigable index).
- **Date —** the calendar date (today) on which you write the handoff.

| Session | Date | Doc | Summary |
|---|---|---|---|
| 1 | 2026-06-12 | _(pre-convention)_ | Initial build: dashboard (visibility), remote control, evolving per-session context files, session indexing. |
| 2 | 2026-06-12 | _(pre-convention)_ | macOS always-on LaunchAgent, cost calculator, launcher/UI, Tailscale phone access (blocked on one Tailscale toggle). |
| 3 | 2026-06-16 | [session-3-2026-06-16.md](handoffs/session-3-2026-06-16.md) | Two-tier indexing rework — Tier-1 free BM25F index over all sessions (`lib/sessionIndex.mjs`, `lib/rank.mjs`) + Tier-2 `context.md` backfill; fixed context-gen outage (Fable-5 gated → default `claude-haiku-4-5` with default-model fallback). |
| 4 | 2026-06-17 | [session-4-2026-06-17.md](handoffs/session-4-2026-06-17.md) | Verified + landed the session-3 indexing rework: `node --check` all 17 `.mjs`, reindex=72 sessions, hasContext 36/72, BM25 `signoz` query precise; gitignored `.serena/`; added this Handoff Log convention. |
| 5 | 2026-06-22 | [session-5-2026-06-22.md](handoffs/session-5-2026-06-22.md) | v1 slice (parallel agents, per-step exit tests) on branch `v1-build`, uncommitted: status-bucketed board + TodoWrite progress + tool-mix/error-rate health + cost rollups/CSV export + A7 dropped fields; LICENSE/package.json+Node≥20 gate, SECURITY.md+token rotation+`CC_REQUIRE_TOKEN_LOCAL`. New `lib/{tasks,health}.mjs`, `cost.mjs` rollup, `/api/cost/rollup`; 12/12 tests green; UI verified via Claude Preview MCP. Plan + results in `docs/V1_BUILD_PLAN.md`. |
| 6 | 2026-06-23 | [session-6-2026-06-23.md](handoffs/session-6-2026-06-23.md) | Independent end-to-end re-verification of the session-5 v1 slice (no new feature code). Re-ran every `docs/V1_BUILD_PLAN.md §4` exit criterion from scratch: 12/12 tests + 19/19 `node --check`; `/api/sessions` tasks/health/A7 fields; cost rollup JSON+CSV; Node<20 gate; token-rotation e2e (old→401, new→302); board + progress + health + cost-modal UI via Claude Preview MCP, no console errors. All green; slice still uncommitted on `v1-build`. |
| 7 | 2026-06-23 | [session-7-2026-06-23.md](handoffs/session-7-2026-06-23.md) | Re-verified the v1 slice once more, then **committed** it (`a9f29c5`) and **pushed** `v1-build` to origin. Built + committed the best value÷effort stretch item **A6 — cross-session prompt palette** (`c60253d`): new `lib/history.mjs` + `/api/history` + palette modal, with secret redaction (login token, API keys, JWT/bearer, DB-URL passwords, underscore env vars) hardened after a 2-agent adversarial review. A3 dropped (sparse `turn_duration`). 19/19 tests; verified via Claude Preview MCP (0 token leaks). `bin/crc` still an unrelated uncommitted change. |
| 8 | 2026-06-23 | [session-8-2026-06-23.md](handoffs/session-8-2026-06-23.md) | Committed `bin/crc` (`3566779`) + opened **PR #1** (`v1-build→master`). Built **C3 — onboarding** (`4dd8043`, **PR #2**): `lib/onboarding.mjs` + `bin/cc-install-hooks` (additive idempotent reversible `settings.json` hook merge) + `bin/cc-doctor` (10-check preflight). Built **C7 — rotating logs + consent-gated bug report** (`140baf5`): `lib/logger.mjs` (0600 rotated log + `bin/cc-logs`) + `lib/diag.mjs` + `/api/diag` + footer link. Each hardened by an adversarial review (C3: backup/relocation/LAN-port/malformed-shape; C7: log perms, loopback-gated error, username scrub). 40/40 tests. Linear PR stack #1←#2←#3; plans in `docs/{C3_ONBOARDING,C7_LOGS_DIAG}_PLAN.md`. v1.0 bar met — only docs polish (FAQ/GIF/CHANGELOG) remains. |
| 9 | 2026-06-24 | [session-9-2026-06-24.md](handoffs/session-9-2026-06-24.md) | Recorded the post-session-8 work the log hadn't captured — **v1.0.0 tag + GitHub release** (`docs polish`, FAQ), the **C7 missing-tests fix** (`6a327b9`, PR #5; CI caught untracked test files), and **C2 — Linux support + GitHub Actions CI matrix** (`2d3aa5e`/`f787620`, PR #7): `lib/platform.mjs` gate, `lib/watch.mjs` Linux poll fallback, cross-platform terminal attach / `start.sh` / systemd `--user` unit, `~/.config/Claude` desktop path; macOS+Linux CI (`node --check`+`node --test`+smoke boot) is the load-bearing Linux verification. Then **cut v1.1.0** (Linux = SemVer minor): `CHANGELOG [1.1.0]` + `package.json` bump + annotated `v1.1.0` tag + GitHub release. CI green on `master`; 43/43 tests, `node --check` clean (25). |
| 10 | 2026-06-25 | [session-10-2026-06-25.md](handoffs/session-10-2026-06-25.md) | Diagnosed + fixed a live `:7433` "Cost over time" crash (4-day-old LaunchAgent served the new disk HTML but 404'd the new routes; frontend read `r.buckets.length` unguarded) — restarted the agent + hardened `loadRollup` (shipped in v1.1.0). Then **cut v1.2.0 — AFK alerts (B-P3)**: `lib/{alerts,notify,config,presence}.mjs` + a gated `setInterval` in `server.mjs` — waiting-on-you digest + once-per-UTC-day budget alert + presence-aware suppression; config-driven & **default-OFF**, injection-safe `osascript`/`notify-send`, `CC_ALERT_DRYRUN`, single-flight state writes (caught a real read-modify-write race in dry-run verify). Built by a worktree agent (**PR #9**), code-reviewed + independently dry-run-verified before merge; 64/64 tests. Renamed to scoped **`@shubhamparashar/cc-orchestrator`** (unscoped taken by `zhsks311`); npm publish **abandoned** (public needs a 2FA OTP, private needs a paid plan) in favor of **install from the private GitHub repo** — `npx github:shubhamparashar/cc-orchestrator`, verified + documented (**PR #11**). Live OS notification verified via the real `osNotify`. |
| 11 | 2026-06-26 | [session-11-2026-06-26.md](handoffs/session-11-2026-06-26.md) | **A9 — sub-agent transcript indexing** (branch `feat/a9-subagent-indexing`). New `lib/subagents.mjs` folds nested `subagents/**/agent-*.jsonl` (incl. `workflows/wf_*/`, ignoring `journal.jsonl`) into both cost + the Tier-1 index from one `(size,mtime)`-cached walk, reusing cost.mjs accumulation (exported `accumulateLine`/`consumeLines`/`accumulateDatedLine`/`mergeUsageByModel`). `attachCost` merges sub-agent usage before pricing (fresh-map merge, no cache mutation); `costRollup` feeds own+sub day×model maps so the cost modal + budget alert include it; `/api/sessions` gains `subagents`/`subagentCost`; `relatedSessions` + `sessionIndex` add `kind:'subagent'` docs (navigate to parent, de-duped); `index.html` gets a "⛬ N subagents" chip + "↳ type: desc" hits. **No double-count proven**: 134 sub-agent assistant ids, 0 overlap with parent; test session cost $552.20→$714.86 (+$162.65 sub-agents); fleet recovered ~$1,080; index 100→654 entries (554 sub-agent, ~5.5×). 87/87 tests (16 new + fixtures). Verified on throwaway PORT=7460 — `:7433` never touched. **Gotcha:** `~/.claude/contexts/index.json` is one shared file — the live `:7433` agent (old code) clobbered the new-format index on its reindex tick; not a correctness bug, but two servers fight over it. |
| 15 | 2026-06-27 | [session-15-2026-06-27.md](handoffs/session-15-2026-06-27.md) | **Finished the backlog — Tasks #5 + #6, three releases.** **v1.6.0** (PR #28): **triage signals (B-P1)** — `lib/health.mjs` derives `writes`/distinct `filesTouched`/`destructiveBash`+`destructiveKinds`/`maxErrorStreak`; frontend `⚠ N destructive` + `⟳ stuck` + `✎ N files`. Destructive detector **tuned against the real 109-session corpus**: only irreversible ops (`git reset --hard`, `git push --force`, `git clean -f`, `drop`/`truncate`, `dd of=`, `mkfs`, `chmod -R 777`) + catastrophic-target rm (root/home/`*`/sudo), per command-segment, `--rm`-flag-safe → ~14% destructive / ~17% stuck. **v1.7.0** (PR #29): **keyboard-first nav (B-P1)** — `j`/`k` ring + `Enter`/`c`/`s`/`f`/`t`/`/`/`Esc`/`?` + `⌨` help dialog; guarded against typing/dialog/focused-control, arrows scroll natively, ring survives refresh. **v1.8.0** (PR #30): **Homebrew formula + CONTRIBUTING (C1)** — `Formula/cc-orchestrator.rb` (`depends_on node`, `brew services`), `brew style`+`ruby -c` clean, install mechanics verified locally; `CONTRIBUTING.md` + README brew line. Per-PR verify (real browser) + `code-reviewer`; reviews caught the proto-guard `'prototype'` gap, destructive-regex false +/− (via corpus smoke), and 3 keyboard edge bugs. 130 tests; CI green; v1.6/1.7 live-verified on `:7433`. **Backlog 1–6 all DONE.** |
| 14 | 2026-06-27 | [session-14-2026-06-27.md](handoffs/session-14-2026-06-27.md) | **Task #4 — roadmap signals (PRODUCT_IDEAS §A), two releases.** **v1.4.0** (PR #25): **A8 status chips** — new `lib/status.mjs` tolerantly reads `~/.claude/{.last-update-result,config,mcp-needs-auth-cache}.json` → `GET /api/status` → header chips (`#ccstatus`) for CC version / approval mode / "N MCPs need re-auth" (names in tooltip, no secrets — id/token never read); **A4** — compaction badge gains a "· ~969k" pre-token suffix from `compactMetadata.preTokens` (`lib/health.mjs` `lastCompactPreTokens` + `fmtTokens`); the "N× compact" half was already shipped under A2 (scout was stale). **v1.5.0** (PR #26): **A5 attribution** — `lib/health.mjs` derives `subagentTypes`/`skills`/`mcpServers` (`Agent.input.subagent_type` / `attributionSkill` / `attributionMcpServer`) in its whole-transcript pass with a `bump()` proto-guard (`key in {}` **plus** `'prototype'` — review's `in {}`-only suggestion missed it, the new test caught it); frontend `📜`/`🔌` badges + by-type subagents tooltip; rides on `s.health`, no new endpoint. 128 tests; per-PR throwaway-port verify agent (real browser) + `code-reviewer`; CI green macOS+Linux; both live-verified on `:7433` (PID 18794→49590→67737). Backlog 1–4 done; 5 (B-P1 UX) + 6 (distribution/docs) remain. |
| 13 | 2026-06-27 | [session-13-2026-06-27.md](handoffs/session-13-2026-06-27.md) | **Security audit complete + 3 releases.** Hand-read all ~16 untrusted-input files (classifier outage blocked parallel agents) — **no exploitable vulns**; surfaces clean (OS-spawn, HTTP/auth/traversal, prototype pollution, ReDoS, redaction). Shipped **v1.3.6** (PR #22, broader `redact()`: Slack webhooks, Google `ya29.`, bare `npm_` tokens), **v1.3.7** (PR #23, per-port session index `indexFileForPort` — two servers no longer clobber `index.json`; default keeps legacy name), **v1.3.8** (PR #24, sub-agent dialogue dedup `dialogueBag`+`termBag` in `rank.mjs`: index −32% 3.57→2.45 MB, sub-agent body −50%, recall improved). Scouted Task #4 (built in session 14). Follow-up: sub-agent docs lack a `work` field (single-mention filenames frequency-evictable). |
| 12 | 2026-06-27 | [session-12-2026-06-27.md](handoffs/session-12-2026-06-27.md) | **Released v1.3.5 — security hardening from PR #20.** The session-11 handoff listed the security audit as "not started," but PR #20 (CSRF-on-loopback, prototype pollution, auth hardening) had merged at 18:45 on 06-26 — *after* that handoff — and was left untagged, undocumented, and **not live**: the `:7433` agent booted 17:47 (pre-merge) so it served stale code. Scope (user): *release what's merged*, not a deeper audit. Kicked the LaunchAgent → fresh PID 81052; proved the fixes live with spawn-safe before/after probes (`POST /api/send` no-`X-CC` **400→403**; invalid `sessionId` → **400 `invalid session id`**; `GET /` 200). 119/119 tests, `node --check` clean. Cut `release/v1.3.5`: `package.json` 1.3.4→1.3.5 + CHANGELOG `[1.3.5]` Security + this log → PR → CI → merge → annotated `v1.3.5` tag + GitHub release. **Left open** (untouched by #20, deferred by scope): OS-spawn-injection review (`actions`/`notify`), secret-redaction review (`history`/`diag`), ReDoS (`scan`), Host-allowlist/CORS/path-traversal confirmation; plus the shared-`index.json` clobber from session-11. |
| 16 | 2026-06-28 | [session-16-2026-06-28.md](handoffs/session-16-2026-06-28.md) | **v1.8.1 — sub-agent `work` field** (PR #32), the one follow-up flagged in session-13's log. Sub-agent search docs only had a frequency-capped 250-term `body`, so a filename named **once** in a tool call could be evicted → the sub-agent was unfindable by it. Sub-agent docs now carry the same unconditionally-kept `work` field session docs already have (file basenames, tool names, leading bash command verbs, spawned sub-agent types), harvested from `tool_use` blocks and scored at the ranker's existing `work` boost (`FIELD_BOOST.work = 4`, **no ranker change**). The harvest (`harvestWorkTokens`/`workString`) was extracted **verbatim** into a shared `lib/work.mjs` that imports only the leaf `lib/scan.mjs`, breaking the would-be `sessionIndex ↔ subagents` cycle; `subagents.docForFile` sets `work`, `sessionIndex.subagentEntries` threads `work: d.work \|\| ''`. New `test/work.test.mjs` (moved harvest tests) + 2 new `subagents.test.mjs` tests proving a tool-only-touched file lands in `work` while staying out of `body`, and that a no-tool sub-agent gets `work === ''`; **132/132**, CI green macOS+Linux. Per-change **verify agent** (real `rankDocs('themeResolver.mjs')` ranks the sub-agent #1; both import orders load clean; real fleekOS sub-agent docs now carry `work`) + **code-reviewer** (extraction byte-identical, cycle broken, no proto surface — one comment-wrap nit, fixed). Annotated `v1.8.1` tag + GitHub release; kickstarted live (PID 29230→60463), reindexed (706 sessions), **live-verified**: `/api/related?q=shipment.ts` now surfaces 3 sub-agents (incl. this session's own verify agent), `q=lineitemstate.ts` 1 — none findable by those filenames before. **Backlog remains fully complete; no open follow-ups.** |
| 17 | 2026-06-28 | [session-17-2026-06-28.md](handoffs/session-17-2026-06-28.md) | **v1.8.2 — two dashboard UI bugs** (PR #34, user-reported), frontend-only (`public/index.html`). **(1) Status filter now collapses the board.** Selecting a status in `#statusFilter` (e.g. `running`) used to keep all four bucket columns + `.colhead`s, the non-matching ones showing "none"; `render()` now drops the `board` class when a filter is active and lays the matches out in the base responsive `#grid` card grid — no other-bucket chrome, only the filtered sessions — restoring the full board when cleared (`applySelection()` runs in both paths so `j`/`k` keeps working). **(2) Dialogs scroll + close.** Expanding the phone panel's "First time?" `<details>` grew the dialog past the viewport; with `overflow:hidden` and no max-height the Close button was clipped, and a phone has no Esc — no way out. `dialog` now caps at `max-height:92vh`, `dialog[open]` is a flex column (scoped to `[open]` so closed dialogs keep the UA `display:none` — the one real risk), `dialog > .dlgpad` scrolls (`overflow-y:auto;min-height:0`), and a backdrop tap (`e.target===d`) dismisses any dialog. 132/132, CI green macOS+Linux. Per-change **verify agent** (real browser on throwaway :7475: filter→0 `.colhead`, Close reachable on a 390×680 phone viewport, Esc+backdrop close, closed dialogs `display:none`, zero console errors, :7433 untouched) + **code-reviewer** (APPROVE; `[open]` scoping + backdrop `e.target` confirmed correct; 2 informational nits, no action). Annotated `v1.8.2` tag + release; kickstarted live (PID 60463→36501), served `index.html` byte-identical to disk. **Still no open backlog; these were ad-hoc user bug reports.** |

| 18 | 2026-06-28 | [session-18-2026-06-28.md](handoffs/session-18-2026-06-28.md) | **v1.9.0 — fork into a live interactive session** (PR #36). Forking from the dashboard ran `claude -p --resume <id> --fork-session` headlessly, so the forked session landed **idle** and never ran — a design gap, not a crash. **(A) Fork ⇢ terminal:** `attachInTerminal`/`attachCommand` (`lib/actions.mjs`) + `/api/attach` (`server.mjs`) gain a `fork` flag that appends `--fork-session` (`Boolean`-coerced server-side); a local-only "Fork ⇢ terminal" card button calls `attach(id, null, true)` to open a live interactive fork in Terminal (`!IS_REMOTE`-gated — a phone can't open a Mac Terminal). **(B) Actionable fork jobs:** completed headless fork jobs render Open / Continue / Terminal on the `forked → <id>` line (`renderJobs()`), with a new `attachFork(forkId, parentId)` helper (parent-cwd fallback) + `.jobacts` row; Continue and attachFork pass `fork=false` so they resume the existing fork id and never re-fork. New `test/actions.test.mjs` (git-added); **135/135** (+3), `node --check` clean. **No real-browser verify this session — Claude Preview MCP offline**; feature-A logic proven by unit tests, UI wiring confirmed by static review (the `fork:true` attach path deliberately not exercised on this Mac). **code-reviewer: APPROVE** (no re-fork, `IS_REMOTE` gating, `fork` threaded end-to-end, `shq()` safe, comments clean). Ship loop: PR #36 → CI green macOS+Linux → `--merge --delete-branch` → annotated `v1.9.0` tag + GitHub release → `launchctl kickstart -k` → live `:7433` on v1.9.0, served `index.html` byte-identical to disk, markers live. Ad-hoc user feature; no open backlog. |
| 19 | 2026-06-28 | [session-19-2026-06-28.md](handoffs/session-19-2026-06-28.md) | **Two user-requested features on top of the session-18 fork work — v1.10.0 + v1.11.0**, one restart at the end took both live. **v1.10.0 — "Fork ⇢ auto"** (PR #38): forking gave a live session but still stopped for every permission prompt, so this adds an autonomous variant. `attachInTerminal`/`attachCommand` (`lib/actions.mjs`) + `/api/attach` (`server.mjs`) gain a `Boolean`-coerced `skipPermissions` flag that appends the static literal `--dangerously-skip-permissions`; a second local-only "Fork ⇢ auto" card button → `attachAuto(id)` `confirm()`s then `attach(id, null, true, true)` (fork + skip), `!IS_REMOTE`-gated so the phone can't trigger it. User chose **full bypass** (not `acceptEdits`) + a **second button**. `test/actions.test.mjs` +2 (skip alone; fork+skip order); **137/137**. **code-reviewer APPROVE** — only `attachAuto` sets the flag, static-literal append (no arbitrary-flag injection), defaults false, `sessionId` still UUID-validated + `shq()`-escaped. **v1.11.0 — click-to-copy chat** (PR #39): each `.bubble` in View chat is click-to-copy via `copyBubble(e, el)` reading `el.textContent` (esc() entities decode on render), guarded on `window.getSelection()` so drag-select still works, with a toast + `cursor:pointer`/`title`/hover; frontend-only, 137/137 unchanged, self-reviewed (textContent not innerHTML, no untrusted input). **No real-browser verify — Claude Preview MCP offline**; both verified via throwaway `:7475` markers + byte-identical-to-disk + (v1.10.0) unit tests & review; the `skipPermissions:true` attach path deliberately not exercised on this Mac. Ship: per-feature PR → CI green macOS+Linux → merge → annotated `v1.10.0`/`v1.11.0` tags + releases → **one** `launchctl kickstart -k` → live `:7433` on v1.11.0, `index.html` byte-identical, all four markers live. No open backlog. |
| 20 | 2026-06-28 | [session-20-2026-06-28.md](handoffs/session-20-2026-06-28.md) | **v1.11.1 — copy confirmation shows on top of the chat dialog** (PR #41), user-reported phone bug following v1.11.0's click-to-copy. The "copied" feedback was a global `toast()` rendered in the bottom-corner `.jobs` panel (`position:fixed; z-index:40`), but the chat panel is a modal `<dialog>` (`showModal()`) the browser paints in the **top layer** — above any z-index — so the toast appeared behind it and was invisible, notably on phone. Fix: feedback now flashes **on the tapped bubble** inside the dialog via `flashBubble(el, label)` (sets `dataset.flash` + `flash` class; CSS `.chatmsg .bubble.flash::after` renders a centered accent pill `content:attr(data-flash)` for ~1.1s; `.bubble` gains `position:relative`); success "copied ✓", failures "no clipboard"/"copy failed"; global toast removed from this path. `el.textContent` read before flashing and the pill is `::after` (not in `textContent`), so re-copy stays exact. Frontend-only, **137/137** unchanged, self-reviewed (no untrusted input, `::after`/dataset not `innerHTML`); **no real-browser verify — Claude Preview MCP offline**, verified via throwaway `:7475` markers + byte-identical-to-disk. Ship: PR #41 → CI green macOS+Linux → merge → annotated `v1.11.1` tag + release → `launchctl kickstart -k` → live `:7433` on v1.11.1, byte-identical, `flashBubble` + flash CSS live. No open backlog. |

_Sessions 1–2 dates are approximate (reconstructed from session context files); 3–8 are exact._
