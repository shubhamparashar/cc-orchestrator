# cc-orchestrator — 2026-06-24 — session 9

_Project: `~/repo/cc-orchestrator`. Architecture in `REPORT.md`; usage in `README.md`; roadmap in
`docs/PRODUCT_IDEAS.md`; per-feature plans + exit results in
`docs/{V1_BUILD_PLAN,C3_ONBOARDING_PLAN,C7_LOGS_DIAG_PLAN,LINUX_SUPPORT_PLAN}.md`; handoff convention
+ log in `CLAUDE.md`. Previous handoff: `handoffs/session-8-2026-06-23.md`._

## What this session did

The handoff log stopped at session 8; three things shipped *after* it (captured in the prior
out-of-repo handoff doc but never in `CLAUDE.md`). This session **recorded that work in the log** and
**cut the v1.1.0 release**.

### Recorded the post-session-8 work (had landed on `master`, was missing from the log)

1. **v1.0.0 tag + GitHub release** — the docs-polish tail from session 8 (troubleshooting/FAQ) plus
   the first tagged release. Tag `v1.0.0` at `2026-06-23`; notes mirror `CHANGELOG.md`.
2. **C7 missing-tests fix** (`6a327b9`, PR #5) — CI caught C7 logger + diag test files that had been
   omitted from the session-8 commit `140baf5`; added them.
3. **C2 — Linux support + GitHub Actions CI matrix** (`2d3aa5e` + hermetic-test fix `f787620`,
   merged via **PR #7** `a00bf03`). Plan + exit tests in `docs/LINUX_SUPPORT_PLAN.md`. Highlights:
   - `lib/platform.mjs` (`isMac`/`isLinux`) gates all Mac-only glue; macOS behavior unchanged.
   - `lib/watch.mjs` — recursive `fs.watch` on macOS; on Linux a ~3 s poll reusing the scanner's
     `(size, mtime)` cache (rejected dir-mtime diffing — misses transcript appends).
   - Cross-platform terminal attach (`lib/actions.mjs`, injection-safe via `shq` + distinct argv
     element), `start.sh` (zsh→bash, `lsof`/`ss`+`/proc`), systemd `--user` always-on unit,
     `~/.config/Claude` desktop path (fail-open, UNVERIFIED).
   - `.github/workflows/ci.yml` — `macos-latest` + `ubuntu-latest` × (`node --check` + `node --test`
     + smoke boot). **This CI is the load-bearing Linux verification** — the tool is built on macOS,
     which cannot boot a Linux runtime. CI also caught a non-hermetic `tasks` test (now fixed).

### Cut v1.1.0 (this session's deliverable)

Linux is a new feature → SemVer minor bump. On branch `release-1.1.0` → PR → merged to `master`:
- `package.json` `version` → `1.1.0`.
- `CHANGELOG.md` `## [1.1.0]` entry (Added: Linux/CI; Changed: gate message; Fixed: hermetic test;
  Docs: Platforms + Demo placeholder) + link reference.
- This handoff doc + the session-9 row in `CLAUDE.md`.
- Annotated tag `v1.1.0` at the post-merge `master` tip; GitHub release with notes mirroring the
  CHANGELOG.

**Pre-release verification (all green):** CI `success` on the `master` tip (PR #7 merge);
`node --test` **43/43**; `node --check` clean on all 25 files. (Linux paths themselves are CI-only —
cannot boot Linux from this Mac.)

## State / roadmap

The v1.0 bar is met and **v1.1.0 (Linux) is released**. The repo's resting state: single `master`
branch, 0 open PRs, CI (macOS + Linux) guarding every push/PR. Not yet built (ranked in
`docs/PRODUCT_IDEAS.md`):

- **Distribution (C1):** publish to npm so `npx cc-orchestrator` works for strangers (`package.json`
  is ready); a Homebrew formula. This is the natural next step now that there's a tagged release.
- **AFK whitespace (B-P3, high value):** aggregate "N waiting on you" digest push + budget-exceeded
  alert + presence-aware suppression — gaps native `claude agents` doesn't cover.
- **Transcript-signal pack (cheap):** A5 sub-agent/skill/MCP attribution, A8 status chips
  (CC version / "N MCPs need re-auth"), A4 compaction markers (count already ships).
- **Bigger:** A9 index the ~387 nested sub-agent transcripts (5.4× search corpus + recover
  unattributed cost); B-P1 keyboard-nav / risk-ranking.
- **Docs:** the **demo GIF** is the one item that needs the *user* to record it — the README "Demo"
  section + the exact markdown to uncomment are already wired (`docs/demo.gif`).

## Access / working style

The user granted **full git/GitHub write access on this repo** and prefers terse confirmations —
commit/push/open-PR/merge/tag/release/prune-branches **without re-asking each time**; just report what
landed. Still: branch off `master` (never commit to the default branch directly), keep commits
focused, run tests + `node --check` (and let CI run) before pushing. This grant is repo-scoped; it
does NOT extend to other external surfaces (Slack, etc.).

## Suggested skills for the next agent
- **`/code-review`** (or `/code-review ultra`) before merging any new PR; **`/security-review`** when
  touching `lib/auth.mjs`, `lib/onboarding.mjs` (settings.json write), `lib/diag.mjs`/`lib/history.mjs`
  (redaction), or the terminal-attach dispatch (`lib/actions.mjs`).
- **`/verify`** / **`/run`** to exercise any runtime change locally before pushing.
- **`/context`** before forking; **`/handoff`** at the next stopping point.

## Environment notes
Node v20.19.5, macOS. Cannot boot Linux locally — trust/extend CI for Linux behavior. `claude` runs
behind the Headroom proxy (port 8787) — large tool outputs may arrive compressed with a
`headroom_retrieve` marker; `command claude` bypasses it. No secrets here.
